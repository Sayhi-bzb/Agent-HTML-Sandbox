# agent-html

agent-html is in the blueprint design stage.

The project explores an agent-human collaboration tool that uses HTML to improve how agents present, share, and refine work with people.

Goals:

- Make artifacts friendly for agent generation and iteration.
- Keep the system lightweight.
- Make outputs easy to share, load, and review.

Current design notes live in `blueprint/`.
