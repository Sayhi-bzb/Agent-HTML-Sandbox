import { benchmarkCopy } from "./benchmark-copy"

export const htmlSource = [
  '<section class="workspace-shell workspace-shell--release">',
  '  <header class="workspace-header">',
  `    <p class="workspace-eyebrow">${benchmarkCopy.summaryStatus}</p>`,
  `    <h1 class="workspace-title">${benchmarkCopy.title}</h1>`,
  `    <p class="workspace-subtitle">${benchmarkCopy.subtitle}</p>`,
  `    <p class="workspace-summary">${benchmarkCopy.summary}</p>`,
  "  </header>",
  "",
  '  <nav class="workspace-tabs" aria-label="Release workspace views" role="tablist">',
  `    <button class="workspace-tab workspace-tab--active" id="tab-summary" aria-selected="true" aria-controls="panel-summary" role="tab">${benchmarkCopy.tabNames.summary}</button>`,
  `    <button class="workspace-tab" id="tab-rollout" aria-selected="false" aria-controls="panel-rollout" role="tab">${benchmarkCopy.tabNames.rollout}</button>`,
  `    <button class="workspace-tab" id="tab-risks" aria-selected="false" aria-controls="panel-risks" role="tab">${benchmarkCopy.tabNames.risks}</button>`,
  "  </nav>",
  "",
  '  <section class="workspace-panels">',
  '    <section class="workspace-panel workspace-panel--active" id="panel-summary" role="tabpanel" aria-labelledby="tab-summary">',
  '      <article class="workspace-card workspace-card--checklist">',
  '        <h2 class="workspace-card-title">Release checklist</h2>',
  '        <ul class="workspace-list">',
  ...benchmarkCopy.checklist.map(
    (item) => `          <li class="workspace-list-item">${item}</li>`,
  ),
  "        </ul>",
  "      </article>",
  "",
  '      <aside class="workspace-alert" role="note" data-tone="warning">',
  '        <h2 class="workspace-alert-title">Recommendation</h2>',
  `        <p class="workspace-alert-copy">${benchmarkCopy.recommendation}</p>`,
  "      </aside>",
  "    </section>",
  "",
  '    <section class="workspace-panel" id="panel-rollout" role="tabpanel" aria-labelledby="tab-rollout" hidden>',
  '      <article class="workspace-card workspace-card--rollout">',
  '        <h2 class="workspace-card-title">Rollout steps</h2>',
  '        <ol class="workspace-list workspace-list--ordered">',
  ...benchmarkCopy.rolloutSteps.map(
    (item) => `          <li class="workspace-list-item">${item}</li>`,
  ),
  "        </ol>",
  "      </article>",
  "",
  '      <table class="workspace-table">',
  "        <thead>",
  "          <tr><th>Evidence</th><th>Status</th></tr>",
  "        </thead>",
  "        <tbody>",
  ...benchmarkCopy.evidence.map(
    ([label, value]) =>
      `          <tr><td class="workspace-cell">${label}</td><td class="workspace-cell">${value}</td></tr>`,
  ),
  "        </tbody>",
  "      </table>",
  "    </section>",
  "",
  '    <section class="workspace-panel" id="panel-risks" role="tabpanel" aria-labelledby="tab-risks" hidden>',
  '      <div class="workspace-accordion" aria-label="Release risks">',
  ...benchmarkCopy.risks.flatMap((risk, index) => [
    `        <section class="workspace-accordion-item" data-risk="${index + 1}">`,
    `          <button class="workspace-accordion-trigger" aria-expanded="${
      index === 0 ? "true" : "false"
    }" aria-controls="risk-panel-${index + 1}">${risk.title}</button>`,
    `          <div class="workspace-accordion-content" id="risk-panel-${index + 1}"${
      index === 0 ? "" : " hidden"
    }>`,
    `            <p class="workspace-accordion-copy">${risk.body}</p>`,
    "          </div>",
    "        </section>",
  ]),
  "      </div>",
  "    </section>",
  "  </section>",
  "</section>",
].join("\n")
