interface:
  display_name: "ahtml"
  short_description: "Turn agent work into readable, shareable HTML artifacts"
  default_prompt: "Use $ahtml to turn agent-facing work into a portable HTML artifact that is easier to read, inspect, share, and hand back into the agent loop."
